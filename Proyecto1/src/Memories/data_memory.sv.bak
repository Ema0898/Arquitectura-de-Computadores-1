module data_memory(input logic clk, we,
						 input logic [19:0] a, wd,
						 output logic [19:0] rd);
						 
  logic [19:0] RAM [127:0];
  assign rd = RAM[a[19:2]]; // word aligned
  
  always_ff @(negedge clk)
    if (we) RAM[a[19:2]] <= wd;
	
endmodule 