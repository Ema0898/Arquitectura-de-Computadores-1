module flag_module	
	#(parameter N = 8)
	(input a, b, msb_sum, carry_out, input logic [N - 1 : 0] result, input logic [3:0] s, output logic [3:0] z);
	
	assign z[3] = result[N - 1];
	assign z[2] = &(~result);
	assign z[1] = carry_out;
	assign z[0] = (a ^ msb_sum) & (~(a ^ b ^ s[0]));
	
endmodule 